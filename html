<!DOCTYPE html>
<html>
<head>
    <meta charset='utf-8'>
    <meta http-equiv='X-UA-Compatible' content='IE=edge'>
    <title>Page Title</title>
    <meta name='viewport' content='width=device-width, initial-scale=1'>
    <link rel='stylesheet' type='text/css' media='screen' href='main.css'>
    <script src='main.js'></script>
</head>
<body>
    <script>
        //ENTRADA DE DADOS
        
        var x = 2;
        var y = 2;
             
        //PROCESSO
        
        var soma = x + y;
        var subtracao = x - y;
        var multiplicacao = x * y;
        var divisao = x / y;
        
        //SAIDA 
        
        alert('A soma dos valores= ' + soma);
        alert('A subtracao dos valores= ' + subtracao);
        alert('A multplicaçao dos valores= ' + multiplicacao);
        alert('A divisao dos valores= ' + divisao);
         //ENTRADA DE DADOS
        
        var x = 50;
        var y = 27.50;
             
        //PROCESSO
        
        var troco = x - y;
        
        
        //SAIDA 
        
        alert('A subtracao dos valores= ' + troco);
        
        //ENTRADA DE DADOS
        
        var x = 10;
        var y = 2;
            
        //PROCESSO
        
        var dimensoes = x * y ;
        
        
        //SAIDA 
        
        alert('As dimensões do retangulo são= ' + dimensoes);
        
        
        //ENTRADA DE DADOS
        
        var anos = prompt ("Qual é a sua idade?");
        var meses = prompt ("E quantos meses?")
        var dias = prompt ("E quantos dias?")
        anos=Number(anos)
        meses=Number(meses)
        dias=Number(dias)
        resultado=Number(resultado)
        //PROCESSO
        
        var imeses = anos * 12;

        var resultado = ((imeses+meses)*30)+ dias;
        
        //SAIDA 
        
        alert('A sua idade em dias é= ' + resultado);
        
        
        //ENTRADA DE DADOS
        
        var salario = prompt ("Qual é o seu salario?");
        var percentual = prompt ("Percentual de reajuste ?")
        
        salario=Number(salario)
        percentual=Number(percentual)
        
        //PROCESSO
        
        var total = (salario * percentual)/100 + salario;
        
        //SAIDA 
        
        alert('Salario final é= ' + total);
        
        
